# Shader
Contains GLSL source files and the Shader class to build them. Also provides functionality for accessing uniforms as well as editing shader code to coincide with system limitations.
This project is still largely a WIP. Must be built as a submodule of the RenderingEngine project.  
